'use strict'

module.exports = (state, { load, type }) => {
  switch (type){
    case 'DOWNLOADING':
      state.purchased = load.purchased
    case 'DOWNLOADING':
      state.purchased = load.purchased
  }
  return state
}